[pytest]
# 保证 `import app` 能解析到仓库根目录的 app.py（无论从哪级目录启动 pytest）
pythonpath = .

# 测开：按标记筛选用例（CI 可拆「契约/冒烟」与全量）
markers =
    smoke: 快速冒烟（核心路径，适合本地/PR 快速反馈）
    contract: API 契约（状态码、JSON 形状、错误体约定）
    integration: 需要本地真实依赖（如 Redis）的集成测试
