apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-deny-all
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redis
  policyTypes:
    - Ingress
  ingress: []
