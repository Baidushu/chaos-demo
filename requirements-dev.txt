# 本地开发、CI（.github/workflows/qa.yml）
-r requirements.txt

pytest>=8.0.0
pytest-cov>=5.0.0
# 基于属性的状态机测试（tests/unit/*_stateful.py：熔断器/限流器）
hypothesis>=6.100.0
# API 契约测试（tests/contract/：OpenAPI 负向生成 fuzz）
schemathesis>=4.20.0
locust>=2.0.0
# api-automation-demo（并入 qa.yml 后对真服务跑 YAML 驱动用例）
httpx>=0.27.0
PyYAML>=6.0.1
allure-pytest>=2.13.5
black>=24.0.0
ruff>=0.6.0
mypy>=1.11.0

# 供应链安全 + 变异测试（.github/workflows/supply-chain.yml、.pre-commit-config.yaml）
pip-audit>=2.7.0
cyclonedx-bom>=7.0.0
mutmut>=3.0.0
pre-commit>=4.0.0
